{
  "name": "auto-mation",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "server:dev": "cd backend && .venv\\Scripts\\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8001"
  }
}
